{% set name = "yacron" %}
{% set version = "0.11.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 5d8da679f3ab503c7871845f3034876af4828aad5ac8eff066413e77ac3bc772

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv
  entry_points:
    - yacron = yacron.__main__:main

requirements:
  host:
    - python >=3.5
    - pip
    - pytest-runner
    - setuptools_scm
  run:
    - python >=3.5
    - strictyaml >=0.7.2
    - crontab
    - aiohttp
    - sentry-sdk
    - aiosmtplib
    - jinja2
    - pytz

test:
  imports:
    - yacron
  commands:
    - yacron --version

about:
  home: https://github.com/gjcarneiro/yacron
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: A modern Cron replacement that is Docker-friendly
  dev_url: https://github.com/gjcarneiro/yacron

extra:
  recipe-maintainers:
    - mcs07
